<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.infrastructure.persistence.mapper.AgentSkillMapper">
    <select id="findAgentsBySkill" resultType="java.util.Map">
        SELECT ask.agent_id, ask.skill_name, ask.skill_level
        FROM agent_skills ask
        WHERE ask.skill_name = #{skillName}
          AND ask.is_active = 1
        ORDER BY FIELD(ask.skill_level, 'senior', 'normal', 'junior')
    </select>
    <select id="findAllActiveAgentSkills" resultType="java.util.Map">
        SELECT ask.agent_id, ask.skill_name, ask.skill_level
        FROM agent_skills ask
        WHERE ask.is_active = 1
        ORDER BY FIELD(ask.skill_level, 'senior', 'normal', 'junior')
    </select>
    <select id="findSkillsByAgentId" resultType="java.lang.String">
        SELECT skill_name FROM agent_skills WHERE agent_id = #{agentId} AND is_active = 1
    </select>
    <insert id="insertSkill">
        INSERT IGNORE INTO agent_skills (agent_id, skill_name, skill_level, is_active)
        VALUES (#{agentId}, #{skillName}, 'normal', 1)
    </insert>
    <delete id="deleteByAgentId">
        DELETE FROM agent_skills WHERE agent_id = #{agentId}
    </delete>
</mapper>
