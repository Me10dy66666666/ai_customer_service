<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.infrastructure.persistence.mapper.SlaWorkCalendarMapper">
    <resultMap id="BaseResultMap" type="com.example.backend.infrastructure.persistence.entity.SlaWorkCalendar">
        <id column="id" property="id"/>
        <result column="calendar_name" property="calendarName"/>
        <result column="work_days" property="workDays"/>
        <result column="work_time_segments" property="workTimeSegments"/>
        <result column="is_active" property="isActive"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    <select id="findAllActive" resultMap="BaseResultMap">SELECT * FROM sla_work_calendar WHERE is_active = 1</select>
    <select id="selectById" resultMap="BaseResultMap">SELECT * FROM sla_work_calendar WHERE id = #{id}</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sla_work_calendar (calendar_name, work_days, work_time_segments, is_active)
        VALUES (#{calendarName}, #{workDays}, #{workTimeSegments}, #{isActive})
    </insert>
    <update id="update">
        UPDATE sla_work_calendar SET calendar_name=#{calendarName}, work_days=#{workDays},
        work_time_segments=#{workTimeSegments}, is_active=#{isActive} WHERE id=#{id}
    </update>
</mapper>
