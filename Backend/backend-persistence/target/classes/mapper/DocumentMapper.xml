<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.mapper.DocumentMapper">

    <resultMap id="BaseResultMap" type="com.example.backend.entity.Document">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="category" property="category" />
        <result column="file_type" property="fileType" />
        <result column="file_path" property="filePath" />
        <result column="dify_document_id" property="difyDocumentId" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, title, content, category, file_type, file_path, dify_document_id, status, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO knowledge_base (
            title,
            content,
            category,
            file_type,
            file_path,
            dify_document_id,
            status,
            create_time,
            update_time
        )
        VALUES (
            #{title},
            #{content},
            #{category},
            #{fileType},
            #{filePath},
            #{difyDocumentId},
            #{status},
            NOW(),
            NOW()
        )
    </insert>

    <update id="update">
        UPDATE knowledge_base
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="category != null">category = #{category},</if>
            <if test="fileType != null">file_type = #{fileType},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="difyDocumentId != null">dify_document_id = #{difyDocumentId},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM knowledge_base WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM knowledge_base WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM knowledge_base
    </select>

    <select id="findAllByOrderByCreateTimeDesc" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM knowledge_base ORDER BY create_time DESC
    </select>

    <select id="existsByTitle" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM knowledge_base WHERE title = #{title})
    </select>

    <select id="findByDifyDocumentId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM knowledge_base WHERE dify_document_id = #{difyDocumentId} LIMIT 1
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM knowledge_base
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM knowledge_base WHERE status = #{status}
    </select>

    <select id="countByCategory" resultType="java.util.Map">
        SELECT category, COUNT(*) AS count FROM knowledge_base GROUP BY category
    </select>

</mapper>
