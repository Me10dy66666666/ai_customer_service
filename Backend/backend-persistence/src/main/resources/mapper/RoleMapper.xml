<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.example.backend.entity.Role">
        <id column="id" property="id" />
        <result column="role_name" property="roleName" />
        <result column="description" property="description" />
    </resultMap>

    <sql id="Base_Column_List">
        id, role_name, description
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (role_name, description)
        VALUES (#{roleName}, #{description})
    </insert>

    <update id="update">
        UPDATE roles
        <set>
            <if test="roleName != null">role_name = #{roleName},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM roles WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM roles WHERE id = #{id}
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM roles
    </select>
    
    <select id="findByRoleName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM roles WHERE role_name = #{roleName}
    </select>

</mapper>
