{
  "name": "@enterprise-webagent/server",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@enterprise-webagent/core": "*",
    "@enterprise-webagent/shared": "*",
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.6.0",
    "zod": "^4.1.5"
  }
}
